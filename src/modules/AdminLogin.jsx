import { useState } from "react";
import { Link, useLocation, useNavigate } from "react-router-dom";
import { loginAdmin } from "../services/authService";
import "../styles/AdminLogin.css";
export default function AdminLogin() { const [form,setForm]=useState({email:"",password:""}),[error,setError]=useState("");const navigate=useNavigate(),location=useLocation();const submit=async e=>{e.preventDefault();setError("");try{await loginAdmin(form);navigate(location.state?.from?.pathname||"/admin/dashboard",{replace:true})}catch(x){setError(x.message)}};return <section className="auth-page"><form className="auth-form admin-form" onSubmit={submit}><p className="eyebrow">Restricted access</p><h1>Admin sign in</h1><p className="form-intro">Manage the BorrowHub library with your administrator account.</p>{error&&<div className="alert alert-danger">{error}</div>}<label>Email address<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required /></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required /></label><button className="btn btn-dark w-100" type="submit">Access admin area</button><Link className="admin-link" to="/login">Return to user sign in</Link></form></section> }
